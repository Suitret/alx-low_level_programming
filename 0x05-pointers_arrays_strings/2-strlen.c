#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: pointer on character
 * Return: returns the length of a string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
